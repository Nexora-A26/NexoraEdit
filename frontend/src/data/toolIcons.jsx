import {
  Archive, ArrowDownAZ, ArrowUpDown, Badge, BarChart3, ClipboardList,
  Code2, Columns3, Combine, CopyX, Eye, FileOutput, FilePlus2,
  FileSearch, FileSpreadsheet, FileText, FileType, Filter, Hash, Heading,
  ImagePlus, Info, Layers, Link, ListOrdered, Merge, PaintBucket,
  RefreshCcw, Replace, RotateCw, Rows3, SearchCheck, Sheet, Sparkles,
  Split, Table, Trash2
} from 'lucide-react';

export const toolIcons = {
  Archive, ArrowDownAZ, ArrowUpDown, Badge, BarChart3, ClipboardList,
  Code2, Columns3, Combine, CopyX, Eye, FileOutput, FilePlus2,
  FileSearch, FileSpreadsheet, FileText, FileType, Filter, Hash, Heading,
  ImagePlus, Info, Layers, Link, ListOrdered, Merge, PaintBucket,
  RefreshCcw, Replace, RotateCw, Rows3, SearchCheck, Sheet, Sparkles,
  Split, Table, Trash2
};

export const DefaultToolIcon = FileText;
